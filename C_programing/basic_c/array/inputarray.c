#include<stdio.h>
int main ()
{
int marks[10];
for (int )


    int marks[10];
    for(int i=0; i<=9; i++){
        printf("enter marks of roll no : %d\n",i+1);
        scanf("%d",&marks[i]);
    } 
      if(marks[10]<35){
        printf("roll no %d\n",marks[i]);
      }
}
return 0;
}

